import * as React from "react";
import * as ReactDOM from "react-dom";
ReactDOM.render(
  <div>
    <h1>Hello, good Welcome to React and TypeScript</h1>
  </div>,
  document.getElementById("root")
);
